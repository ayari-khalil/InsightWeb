{% extends 'default.html.twig' %}

{% block title %}New Tests{% endblock %}

{% block body %}
  
<div class="container">
    <div class="row justify-content-center">
        <h1 class="text-center">Create new Test</h1>
        {{ include('tests/_form.html.twig') }}
        <div class="col-md-2">
            <a href="{{ path('app_tests_index') }}" class="btn btn-secondary btn-block mt-3">Back to List</a>
        </div>   
    </div>
</div>
{% endblock %}
