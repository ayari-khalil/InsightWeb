{% extends 'default.html.twig' %}

{% block title %}Tests{% endblock %}

{% block body %}
 
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <h1 class="text-center">Test</h1>

            <table class="table">
        <tbody>
            <tr>
                <th>Test_id</th>
                <td>{{ test.test_Id }}</td>
            </tr>
            <tr>
                <th>Duree</th>
                <td>{{ test.duree }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ test.note }}</td>
            </tr>
            <tr>
                <th>Matiere</th>
                <td>{{ test.matiere }}</td>
            </tr>
        </tbody>
    </table>
    <div class="row justify-content-center">
    <div class="col-md-5">
    <a href="{{ path('app_tests_index') }}" class="btn btn-success btn-block mt-3">back to list</a>

    <a href="{{ path('app_tests_edit', {'test_id': test.test_id}) }}" class="btn btn-primary btn-block mt-3">edit</a>
    </div>
    </div>

    {{ include('tests/_delete_form.html.twig') }}
    </div></div></div>
{% endblock %}
