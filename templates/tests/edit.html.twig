{% extends 'back.html.twig' %}

{% block title %}Edit Tests{% endblock %}

{% block body %}
<div class="row justify-content-center">
            <div class="col-md-5">
    <h1>Edit Tests</h1>
    </div>
        </div>

    {{ include('tests/_form.html.twig', {'button_label': 'Update'}) }}
<div class="row justify-content-center">
            <div class="col-md-5">
    <a href="{{ path('app_tests_index') }}" class="btn btn-success btn-block mt-3">back to list</a>
    </div>
        </div>

    {{ include('tests/_delete_form.html.twig') }}
{% endblock %}
