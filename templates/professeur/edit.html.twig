{% extends 'BackOffice/adminDashboard.html.twig' %}

{% block title %}Edit Professeur{% endblock %}

{% block main_content %}
    <div class="container-fluid pt-4 px-4">
        <div class="row justify-content-center">
            <div class="col-lg-8">
                <div class="h-100 bg-light rounded p-4">
                    <h1>Edit Professeur</h1>

                    {{ include('professeur/_form.html.twig', {'button_label': 'Update'}) }}

                    <a href="{{ path('app_professeur_index') }}" class="btn btn-secondary">Back to List</a>

                    {{ include('professeur/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
