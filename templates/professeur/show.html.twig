{% extends 'base.html.twig' %}

{% block title %}Professeur{% endblock %}

{% block body %}
    <h1>Professeur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ professeur.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ professeur.email }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ professeur.password }}</td>
            </tr>
            <tr>
                <th>FirstName</th>
                <td>{{ professeur.firstName }}</td>
            </tr>
            <tr>
                <th>LastName</th>
                <td>{{ professeur.lastName }}</td>
            </tr>
            <tr>
                <th>BirthDate</th>
                <td>{{ professeur.birthDate ? professeur.birthDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>PhoneNumber</th>
                <td>{{ professeur.phoneNumber }}</td>
            </tr>
            <tr>
                <th>Cin</th>
                <td>{{ professeur.cin }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ professeur.role }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_professeur_index') }}">back to list</a>

    <a href="{{ path('app_professeur_edit', {'id': professeur.id}) }}">edit</a>

    {{ include('professeur/_delete_form.html.twig') }}
{% endblock %}
