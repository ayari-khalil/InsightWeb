{% extends 'base.html.twig' %}

{% block title %}Admin{% endblock %}

{% block body %}
    <h1>Admin</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ admin.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ admin.email }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ admin.password }}</td>
            </tr>
            <tr>
                <th>FirstName</th>
                <td>{{ admin.firstName }}</td>
            </tr>
            <tr>
                <th>LastName</th>
                <td>{{ admin.lastName }}</td>
            </tr>
            <tr>
                <th>BirthDate</th>
                <td>{{ admin.birthDate ? admin.birthDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>PhoneNumber</th>
                <td>{{ admin.phoneNumber }}</td>
            </tr>
            <tr>
                <th>Cin</th>
                <td>{{ admin.cin }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ admin.role }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_index') }}">back to list</a>

    <a href="{{ path('app_admin_edit', {'id': admin.id}) }}">edit</a>

    {{ include('admin/_delete_form.html.twig') }}
{% endblock %}
